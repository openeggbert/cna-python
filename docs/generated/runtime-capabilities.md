# Runtime capability inventory

CNA C ABI 0.21.0, cnanext `5347b52eae13`, Sharp Runtime `9cc96cd57cde`.

A status is a claim about a measured artifact, never about CNA in general.
Where a row differs between artifacts the breakdown is shown; where it does not,
the status held on every artifact it was measured on.

## Qualified artifacts

| Id | Role | Renderer | Rasterizes | Audio | Video | Platform | SHA-256 |
|---|---|---|---|---|---|---|---|
| `control` | deterministic non-windowed control | HEADLESS | no | SDL3 mixer on the dummy device | AUTO (FFmpeg present) | Linux x86-64 | `94078be94dc1f1e6…` |
| `gpu` | real windowed renderer | OPENGLES3 | yes | SDL3 mixer on the dummy device | AUTO (FFmpeg present) | Linux x86-64, isolated Xvfb display, Mesa GL ES 3.2 (llvmpipe) | `65ce46a49b754586…` |

## Totals

```text
CAPABILITIES=208
VERIFIED_NATIVE=163
VERIFIED_MANAGED=11
BLOCKED_UPSTREAM=12
BLOCKED_RENDERER=3
BLOCKED_PLATFORM=5
BLOCKED_HARDWARE=2
BLOCKED_FIXTURE=6
LANGUAGE_MAPPING_LIMITATION=3
NOT_USEFUL_FOR_PYTHON=1
DELIBERATE_OUT_OF_SCOPE=2
ACTIONABLE_LOCAL=0
```

## Operations

| Operation | Status | By artifact | Evidence | Notes |
|---|---|---|---|---|
| FrameworkDispatcher explicit Update | VERIFIED_NATIVE | same on all | cna_framework_dispatcher_update; Milestone-8 dispatcher tests | One explicit call adds one pump to the existing single automatic post-Update pump and re-raises contained callback failures after native return. |
| FrameworkDispatcher Update without a live Game | BLOCKED_UPSTREAM | same on all | runtime.h cna_framework_dispatcher_update(CNA_Handle game); re-read on CNA 0.21.0 | Re-measured, unchanged: the route still takes a Game handle, so it has nothing to pump without one. XNA permits a process-level call; Python reports the limitation and never silently no-ops. |
| GamerServicesComponent lifecycle | VERIFIED_NATIVE | same on all | cna_gamer_services_dispatcher_set_window_handle/initialize/update | The normal GameComponent initialization/update order uses the Game window and current native generation. |
| Broader GamerServices ecosystem | DELIBERATE_OUT_OF_SCOPE | same on all | the selected 257-type profile ends at GamerServicesComponent | Not a backend limit: CNA has 250 gamer-service routes and the profile deliberately excludes them. Opening the family is a future-profile decision, and no Gamer, Guide, Avatar, achievement or leaderboard facade is fabricated meanwhile. |
| Game.Components/services/traversal | VERIFIED_MANAGED | same on all | tests.test_game_graphics_foundation.GameObjectModelTests | Per-Game services and snapshot traversal with stable ordering. |
| Game.ResetElapsedTime/SuppressDraw | VERIFIED_NATIVE | same on all | cna_game_reset_elapsed_time; cna_game_suppress_draw | Calls the active native Game lifecycle. |
| Game activation/deactivation delivery | VERIFIED_NATIVE | same on all | cna_game_subscribe; the Activated event is delivered on both qualified artifacts | Activation is delivered for real. A deactivation transition needs a focus change no automated run performs, so it is exercised but not observed here. |
| GameWindow properties and screen-device change | VERIFIED_NATIVE | same on all | runtime_window.h routes | The control artifact reports its real null-window state; the windowed renderer reports a real 800x480 client bounds and a real screen device name. |
| GameWindow resize/orientation events | VERIFIED_NATIVE | control: BLOCKED_RENDERER; gpu: VERIFIED_NATIVE | cna_game_window_subscribe; ClientSizeChanged delivered after ApplyChanges resized the window from 800x480 to 512x384 on the windowed artifact | The event is real on a renderer that has a window. The control artifact has none, so it delivers nothing and none is synthesized. Orientation never changes on this platform. |
| SurfaceFormat and graphics value enums | VERIFIED_MANAGED | same on all | behavior graphics.surface_format.values | Values follow XNA metadata, independently of CNA enum ordering. |
| Viewport.Project/Unproject | VERIFIED_MANAGED | same on all | behavior graphics.viewport.* | Binary32 XNA operation ordering. |
| GraphicsResource disposal event | VERIFIED_NATIVE | same on all | cna_graphics_resource_subscribe_disposing | Exactly-once event before native release; double Dispose is idempotent. |
| GraphicsDevice.ResourceCreated/ResourceDestroyed | BLOCKED_UPSTREAM | same on all | graphics_device.h CNA_ResourceCreatedEventInfo / CNA_ResourceDestroyedEventInfo | The subscription routes now exist, but CNA states that the event is raised from the graphics-resource base constructor, so the object is still under construction and no native object pointer crosses the ABI. XNA's ResourceCreatedEventArgs.Resource therefore cannot be supplied and would have to be fabricated. |
| Graphics adapter/display/presentation queries | VERIFIED_NATIVE | same on all | display.h routes and native integration | Durable adapter and display facades. Each artifact reports its own single adapter. |
| Blend/depth/rasterizer/sampler state binding | VERIFIED_NATIVE | same on all | graphics_state.h and native foundation test | XNA defaults and post-bind freeze are managed; descriptors are copied by CNA. |
| SamplerStateCollection/TextureCollection | VERIFIED_NATIVE | same on all | graphics_device.h state/texture slot routes | Durable per-device stage facades; Reach exposes zero vertex-stage slots. |
| Texture metadata and Color transfer | VERIFIED_NATIVE | same on all | texture.h and native tests | Format and level count are read from native resource information. |
| Texture2D PNG/JPEG encoding | VERIFIED_NATIVE | same on all | cna_texture2d_*encoded* and native foundation test | Uses CNA encoders with writable-stream validation and rollback. |
| Vertex declarations and built-in codecs | VERIFIED_MANAGED | same on all | graphics.vertex.strides; explicit struct.pack codecs | No reflection, pickle, JSON, or __dict__ layout inference. |
| Vertex/Index buffer create, transfer, binding | VERIFIED_NATIVE | same on all | vertex_resources.h; index_resources.h; native foundation/stress | Owned handles; native binding borrows and bound disposal is rejected. |
| DynamicVertexBuffer.SetData(offset, streaming options) | VERIFIED_NATIVE | same on all | cna_vertex_buffer_set_data_raw_at_with_options; a window written at vertex three with NoOverwrite left vertices zero to two intact | Closed upstream: the current generation has the route carrying both the destination offset and the streaming hint, which the historical one lacked. |
| DrawPrimitives/DrawIndexedPrimitives | VERIFIED_NATIVE | control: BLOCKED_RENDERER; gpu: VERIFIED_NATIVE | rendered-pixel test: each path writes its own colour and reads it back | Both paths produce the colour they were given on the windowed renderer. The control artifact has no pixel storage and refuses back-buffer readback outright. |
| DrawInstancedPrimitives | VERIFIED_NATIVE | control: BLOCKED_RENDERER; gpu: VERIFIED_NATIVE | cna_graphics_device_draw_instanced_primitives; two instances drawn and read back | Executes and rasterizes on the windowed renderer. |
| DrawUserPrimitives/DrawUserIndexedPrimitives | VERIFIED_NATIVE | control: BLOCKED_RENDERER; gpu: VERIFIED_NATIVE | rendered-pixel test: each user path writes its own colour and reads it back | Both user-array paths rasterize. Only explicit built-in vertex codecs and deterministic contiguous bytes are accepted. |
| RenderTarget2D create/bind/query | VERIFIED_NATIVE | same on all | render_target.h and native foundation/stress | Create, bind and query are verified on both artifacts; the pixels a target keeps are verified separately on the renderer that rasterizes. |
| RenderTarget visible output | VERIFIED_NATIVE | control: BLOCKED_RENDERER; gpu: VERIFIED_NATIVE | a render target cleared to a known colour reads that colour back through GetData, and the backbuffer keeps its own | Rendering into a target and reading it back is verified; the two surfaces are shown to be distinct rather than assumed. |
| OcclusionQuery | VERIFIED_NATIVE | same on all | cna_occlusion_query_*; the query completes asynchronously and PixelCount reads after it | XNA's query is asynchronous. The control artifact answers at once and the windowed renderer takes a bounded number of polls; both satisfy the contract, so the test waits rather than encoding one backend's timing. |
| RenderTargetCube create/bind/query | VERIFIED_NATIVE | same on all | cna_render_target_cube_create; ordinary render-target binding routes; Milestone-8 native tests | Both constructors preserve format/depth/MSAA/usage, all six faces round-trip through GetRenderTargets, and the type retains TextureCube ownership. |
| RenderTargetCube current content-loss state | VERIFIED_NATIVE | same on all | cna_render_target_get_info.is_content_lost | The current state is queried from CNA rather than tracked in Python; each artifact reports its own. |
| RenderTargetCube ContentLost delivery | BLOCKED_RENDERER | same on all | cna_render_target_subscribe_content_lost / _unsubscribe_content_lost | Subscription and release are now real and verified on both artifacts. CNA raises the event only where a renderer can lose and recreate its device, which none of the identities available on this platform can, so a subscription here is valid and silent. No loss is synthesized. |
| Bound render-target disposal | VERIFIED_NATIVE | same on all | cna_render_target_destroy returns CNA_RESULT_INVALID_STATE, 'Disposing target that is still bound'; the same handle destroys cleanly once unbound | Closed upstream: the historical generation aborted the process here, which is why Python guards before calling. CNA now refuses cleanly, and the Python guard is kept as defence in depth. It preserves the handle for a legal retry and never silently restores the backbuffer. |
| GraphicsDevice.Present() | VERIFIED_NATIVE | same on all | cna_graphics_device_present and 60/600-frame consumers on both artifacts | Present completes on both artifacts. On the renderer that rasterizes, the starter's frame check reads the back buffer afterwards and finds the drawn frame; on the control artifact there is no pixel storage to read, and completion is not presentation evidence. |
| GraphicsDevice.Present(rectangles, window) | BLOCKED_UPSTREAM | same on all | graphics_device.h cna_graphics_device_present(CNA_Handle graphics_device) | Re-measured, unchanged: the only Present route takes the device alone, with no source or destination rectangle and no target window. Raises after strict argument validation. |
| GraphicsDevice.Reset events | VERIFIED_NATIVE | same on all | cna_graphics_device_reset* and event subscriptions | Resetting and Reset are delivered by actual native transitions. |
| GraphicsDevice.DeviceLost | BLOCKED_RENDERER | same on all | cna_graphics_device_subscribe_event; no device-loss transition exists on this renderer | The subscription is real. Only renderer families whose API can lose a device raise it, and none is available on this platform; none is fabricated. |
| GraphicsDeviceManager lifecycle/preparing settings | VERIFIED_NATIVE | same on all | runtime_graphics_manager.h routes | Mutable preparation callback writes validated settings back to CNA. |
| SpriteFont metrics and DrawString | VERIFIED_NATIVE | same on all | sprite_font.h plus SpriteBatch glyph submissions | Private glyph factory is used until Content/XNB can construct public assets. |
| SpriteFont public Content loading | VERIFIED_NATIVE | same on all | SpriteFontReader graph; tests.test_content_native.NativeContentTests | Real reader-table graph constructs the existing native SpriteFont, verifies metrics/DrawString, cache identity, and reverse atlas ownership. |
| ContentManager.OpenStream / TitleContainer.OpenStream | VERIFIED_NATIVE | same on all | cna_title_location_set_path_ext; cna_title_container_read_ext; managed path suite | Deterministic application title root with lexical and symlink escape rejection; no process-CWD fallback. |
| ContentManager.ReadAsset/XNB | VERIFIED_NATIVE | same on all | managed ContentReader suite plus native Texture2D/SpriteFont/buffer graphs | Header, exact reader resolver, versions, custom readers, shared fixups, external references, cache, rollback, and reverse disposal are exercised. |
| XNB LZX framing and decoder | VERIFIED_MANAGED | same on all | single/multi-frame synthetic suite and independent MonoGame fixture comparison | Persistent 64 KiB state, short/extended framing, exact declared output, malformed boundaries, and compressed resource graphs. |
| ResourceContentManager | VERIFIED_MANAGED | same on all | Mapping/GetObject adapter and stream ownership tests | No public System.Resources support package is introduced. |
| Content Load[T] caller type enforcement | LANGUAGE_MAPPING_LIMITATION | same on all | docs/xna-python-mapping.md generic-erasure rule | The root reader determines runtime shape; Python cannot recover erased caller T, while reader-declared target shape remains validated. |
| ContentReader.ReadRawObject[T] without reader token | LANGUAGE_MAPPING_LIMITATION | same on all | tests.test_content.ContentTests | No assignment-context, caller-bytecode, asset-name, or locals inference; explicit-reader overloads are functional. |
| Effect reflection ownership graph | VERIFIED_NATIVE | same on all | native identity integration and effect-model ownership stress | Effect is the sole owner; parameter, annotation, technique, pass, light, and collection handles are stable owned views retained by and invalidated with the Effect. |
| Effect typed parameters | VERIFIED_NATIVE | same on all | cna_effect_parameter_get/set_value*, native codec integration | Boolean, Int32, Single, String, Vector2/3/4, Quaternion, Matrix/transpose, all selected arrays, and Texture2D/Cube identities use copied typed storage. |
| Effect annotations | VERIFIED_NATIVE | same on all | cna_effect_annotation_get_value_* integration | Read-only scalar, string, vector, and matrix codecs use real annotation views and metadata validation. |
| EffectPass.Apply | VERIFIED_NATIVE | same on all | cna_effect_pass_apply; stock and Model-XNB draw tests | A real native pass identity is applied repeatedly; applying after parent disposal is rejected. |
| Compiled Effect creation route | VERIFIED_NATIVE | same on all | cna_effect_create_compiled with project-authored legal conformance FXB SHA-256 2e1fe1dd74d67f4395ae6db4451c1a19ee4478dfe7906f9d665a499e28d2a074 | The byte buffer reaches CNA with exact ownership rollback; route verification is distinct from backend acceptance. |
| Compiled Effect execution | BLOCKED_FIXTURE | control: BLOCKED_RENDERER; gpu: BLOCKED_FIXTURE | cna_graphics_device_executes_shader_effect_source_ext answers true on the windowed renderer and false on the control artifact | The running device is asked rather than the renderer name inferred from. The windowed renderer does execute shader effect source, so what is missing is a legal compiled-effect fixture, not backend support. No shader is fabricated. |
| BasicEffect | VERIFIED_NATIVE | same on all | cna_basic_effect_* property/create routes and cna_effect_pass_apply | Native state, directional lights, clone isolation, texture retention, and Apply are verified; visible pixels are not. |
| AlphaTestEffect | VERIFIED_NATIVE | same on all | cna_alpha_test_effect_* and cna_effect_pass_apply | Native properties, texture retention, clone/disposal route, and Apply are verified. |
| DualTextureEffect | VERIFIED_NATIVE | same on all | cna_dual_texture_effect_* and cna_effect_pass_apply | Both native texture slots, managed identity retention, state, and Apply are verified. |
| EnvironmentMapEffect | VERIFIED_NATIVE | same on all | cna_environment_map_effect_* and cna_effect_pass_apply | Texture2D/TextureCube identities, lights/fog/matrices, state, and Apply are verified. |
| SkinnedEffect | VERIFIED_NATIVE | same on all | cna_skinned_effect_* and cna_effect_pass_apply | Exact 1..72 bone-transform transfer, weights, lights/fog/matrices, state, and Apply are verified. |
| EffectMaterial | VERIFIED_NATIVE | same on all | cna_effect_material_create | The material owns a distinct native Effect cloned from its source. |
| Stock-effect visible GPU output | VERIFIED_NATIVE | control: BLOCKED_RENDERER; gpu: VERIFIED_NATIVE | BasicEffect with vertex colours produces the drawn colour through every draw path | Stock-effect output is rendered and read back. |
| Texture3D | VERIFIED_NATIVE | control: BLOCKED_RENDERER; gpu: VERIFIED_NATIVE | cna_texture3d_create/set_data/get_data; all eight voxels round-trip exactly | Creation and the complete Color codec work on the windowed renderer. The control artifact refuses creation, which is a renderer boundary rather than a missing feature. |
| TextureCube creation and metadata | VERIFIED_NATIVE | same on all | cna_texturecube_create/get_info and repeated lifecycle tests | All six faces and mip/rectangle contracts are implemented over the canonical ABI. |
| TextureCube Color transfer | VERIFIED_NATIVE | control: BLOCKED_RENDERER; gpu: VERIFIED_NATIVE | cna_texturecube_set_data/get_data; two faces written with different contents read back distinctly | The face selector is proven by writing two faces differently rather than by writing one. |
| Model graph | VERIFIED_MANAGED | same on all | Model collection/identity behavior and direct graph stress | Bones, hierarchy, meshes, parts, effects, tags, transform copies, and deterministic invalidation are verified. |
| Model.Draw command path | VERIFIED_NATIVE | same on all | XNB Model -> buffers -> BasicEffect -> native pass -> cna_graphics_device_draw_indexed_primitives | The ordinary indexed route accepts the command on both artifacts. The pixels it produces are a separate row, verified on the renderer that rasterizes. |
| Model visible GPU output | VERIFIED_NATIVE | control: BLOCKED_RENDERER; gpu: VERIFIED_NATIVE | a Model loaded from XNB and drawn through its BasicEffect covered 47,960 pixels | The managed XNB model graph draws for real. The graph itself stays managed by design; CNA's native model runtime is a separate extension concept and does not replace it. |
| Model XNB | VERIFIED_NATIVE | same on all | legal synthetic Windows XNB v5 graph integration | Two bones, one mesh, two parts, shared native buffers/BasicEffect, tags, cache, Unload/reload, external reference, and rollback are verified. |
| Compressed Model XNB | VERIFIED_NATIVE | same on all | same legal Model payload through existing managed LZX framing | The same reader table, reader, public graph, shared native resources, draw, cache, Unload/reload, and rollback paths are used. |
| SoundEffect construction/playback | VERIFIED_NATIVE | same on all | cna_sound_effect_create_pcm16_range_ext; cna_sound_effect_create_from_encoded_ext; cna_sound_effect_play*; tests.test_audio.AudioNativeTests | Raw PCM16 and legal project-authored RIFF/WAVE data reach CNA with copied input and transactional ownership. NULL audio accepts construction but produces no audible-output claim. |
| SoundEffectInstance | VERIFIED_NATIVE | same on all | cna_sound_effect_create_instance; cna_sound_effect_instance_*; Audio ownership stress | Playback/state methods use native state; Volume, Pitch, Pan, and IsLooped retain XNA's cached read-after-disposal behavior. |
| SoundEffectInstance.Apply3D(single listener) | VERIFIED_NATIVE | same on all | cna_sound_effect_instance_apply_3d | The listener and emitter are copied into the exact native route. |
| SoundEffectInstance.Apply3D(multiple listeners) | VERIFIED_NATIVE | same on all | cna_sound_effect_instance_apply_3d_multi_ext accepts any positive listener count | Closed upstream since ABI 0.9: the historical generation refused every count but one. Where several listeners are given the nearest decides the applied attenuation, pan and Doppler, which is an approximation CNA documents rather than one this binding invents. A zero count stays refused and is not guessed at. |
| DynamicSoundEffectInstance | VERIFIED_NATIVE | same on all | cna_dynamic_sound_effect_instance_*; tests.test_audio; Audio ownership stress | Submitted ranges are copied by CNA, pending count and sample helpers are native, and no Python buffer retention is required. |
| DynamicSoundEffectInstance.BufferNeeded | VERIFIED_NATIVE | same on all | cna_dynamic_sound_effect_instance_subscribe_buffer_needed; cna_framework_dispatcher_update | CNA documents and testing confirms delivery on the Game thread during its dispatcher pump. The trampoline verifies owner-thread identity, contains exceptions in Python, and registrations are removed before disposal. |
| Microphone enumeration | VERIFIED_NATIVE | same on all | cna_microphone_get_count and the indexed name/rate/state routes; the real mixer enumerates capture devices and reports a default | The historical null backend had no devices at all. Enumeration, naming, sample rate and default selection are now measured against a real backend. |
| Microphone capture | BLOCKED_HARDWARE | same on all | Start/Stop transition the state machine, BufferDuration validation refuses a 10 ms duration, GetSampleSizeInBytes matches the reported rate, and GetData returns bytes | The capture path is fully exercised: the state machine, the duration contract, the size arithmetic and a real byte transfer. The bytes are silence because the deterministic device supplies silence, so a non-zero sample remains physical-capture evidence this environment cannot produce. |
| AudioEngine | VERIFIED_NATIVE | same on all | cna_audio_engine_create* real malformed-XGS error routes and rollback | The complete public root and owned child graph are implemented. No successful-engine claim is made without a legal authored XGS fixture. |
| AudioEngine renderer selection | BLOCKED_UPSTREAM | same on all | xact.h: 'Both extra arguments are accepted and ignored' | Re-measured, unchanged: CNA has exactly one audio backend, so a renderer id has nothing to select between. Reported rather than approximated. |
| AudioEngine look-ahead | BLOCKED_UPSTREAM | same on all | xact.h: 'Both extra arguments are accepted and ignored' | Re-measured, unchanged: the backend has no scheduling look-ahead to set. |
| RendererDetail | VERIFIED_MANAGED | same on all | audio.renderer.default behavior observation; renderer enumeration ABI routes | XNA default null strings, equality, hash, copy, and type-name ToString behavior are verified. NULL audio yields no fabricated renderer. |
| AudioCategory | BLOCKED_FIXTURE | same on all | cna_audio_engine_get_category; cna_audio_category_* exact ABI and managed default-value tests | The parent-owned value facade, stable cache, default semantics, and invalidation are complete; successful category operations require legal authored XGS data. |
| WaveBank | VERIFIED_NATIVE | same on all | cna_wave_bank_create* failed-create routes; Audio ownership stress | Both constructor shapes, rollback, ownership, and disposal are verified through CNA. Successful authored-bank loading remains asset-pending. |
| SoundBank | VERIFIED_NATIVE | same on all | cna_sound_bank_create failed-create route; Audio ownership stress | Construction failure, rollback, ownership, and disposal are verified through CNA. Successful authored-bank loading remains asset-pending. |
| Cue | BLOCKED_FIXTURE | same on all | complete cna_cue_* ABI and SoundBank-owned lifecycle implementation | The full facade and ownership path are present, but normal acquisition and method execution require a legal authored SoundBank fixture. |
| Authored XACT playback | BLOCKED_FIXTURE | same on all | repository/reference fixture audit found no legal redistributable XGS/XSB/XWB set | No asset is fabricated and the absence of a legal fixture is not classified as a CNA defect. |
| Touch value types and TouchCollection | VERIFIED_MANAGED | same on all | behavior touch.*; tests.test_milestone8.Milestone8ManagedTests | Exact flags, value copies, XNA equality distinction, read-only collection, and nested Enumerator boundaries are exercised. |
| Touch capabilities and state | VERIFIED_NATIVE | same on all | cna_touch_get_capabilities/state | The qualified runtime honestly reports no connected physical panel and an empty snapshot. |
| TouchPanel static configuration | VERIFIED_NATIVE | same on all | cna_touch_panel_get/set_* round trips | Gestures, window IntPtr, display orientation, width, and height use the current Game generation with XNA validation. |
| Physical touch input | BLOCKED_HARDWARE | same on all | neither qualified artifact has a physical touch source | An empty native state is not treated as hardware qualification and no touches are fabricated. |
| Touch gesture recognition | BLOCKED_PLATFORM | same on all | real gesture availability/read routes; qualified queue is empty | The no-gesture exception path is verified; positive recognition requires a platform event source. |
| Storage selector operation | VERIFIED_NATIVE | same on all | all four cna_storage_device_show_selector* routes | CNA completes synchronously and deterministically returns its configured device; Python reports that timing and does not claim UI was shown. |
| Storage selector UI | BLOCKED_PLATFORM | same on all | CNA 0.7 selector implementation has no OS UI | No selector interface is fabricated. |
| Storage async Begin/End mapping | VERIFIED_NATIVE | same on all | opaque one-shot result provenance and callback tests | Same-token callbacks, state retention, operation/device/generation checks, double-End rejection, and exception containment are verified. |
| Storage filesystem and streams | VERIFIED_NATIVE | same on all | cna_storage_container_* and cna_storage_stream_* integration/stress | Directory/file/pattern operations and capability-based read/write/seek/flush/close use CNA exclusively with exact ownership. |
| Storage path containment | VERIFIED_MANAGED | same on all | lexical/resolved containment tests before CNA dispatch | Absolute, drive, mixed-separator, NUL, lexical .., and symlink escapes are rejected, closing CNA 0.7's traversal mismatch. |
| Storage FileShare enforcement | BLOCKED_UPSTREAM | same on all | storage.h: 'The canonical implementation currently ignores file_share' | Re-measured, unchanged. The exact flag value reaches the C ABI and Python reports the divergence instead of pretending the lock was taken. |
| StorageContainer.Disposing | VERIFIED_NATIVE | same on all | cna_storage_container_subscribe_disposing and exactly-once lifecycle tests | The real native callback is observed before Python handler delivery; double Dispose is idempotent and self-removal is safe. |
| StorageDevice.DeviceChanged registration | VERIFIED_NATIVE | same on all | cna_storage_device_subscribe/unsubscribe_device_changed | Registration lifetime and shared dispatcher queue delivery are bound without exposing a callback through ctypes. |
| Real storage-device transitions | BLOCKED_PLATFORM | same on all | qualified host exposes no deterministic attach/remove transition | No DeviceChanged event is fabricated. |
| Media enum and VisualizationData values | VERIFIED_MANAGED | same on all | 10 PURE_XNA_DERIVED Media observations; independent ctypes/C layout probe | Exact sparse enum identities and two stable immutable 256-Single snapshots; no ctypes buffer escapes. |
| Media read-only collections | VERIFIED_NATIVE | same on all | seven cna_*_collection_* route groups; tests.test_media; Media ownership stress | Native ordering, bounds, snapshot iteration, stable item facades, disposal-to-empty, and generation invalidation are verified. |
| Artist/Album/Genre/Playlist graph | VERIFIED_NATIVE | same on all | canonical catalog relationship routes and shared identity domain | The qualified music provider is empty; picture-album relationship identity supplies positive graph evidence without invented music entries. |
| MediaLibrary lifecycle | VERIFIED_NATIVE | same on all | cna_media_library_create/dispose/destroy; 20 lifecycle and wrong-thread/retry cycles | The library owns its provider handle; child handles retain provider state, public Dispose preserves documented readable metadata, and generation teardown destroys children first. |
| MediaLibrary music catalog | VERIFIED_NATIVE | same on all | qualified Local Device provider returned zero Songs/Albums/Artists/Genres/Playlists | This verifies the real empty provider result on this host only; it is not a claim about other platforms or user libraries. |
| MediaLibrary pictures | VERIFIED_NATIVE | same on all | qualified provider returned native Picture/PictureAlbum metadata and stable relationships | Names, dimensions, dates, nested album ordering, disposal, and identity are native; no catalog entry is fabricated. |
| Picture image/thumbnail/token | BLOCKED_PLATFORM | same on all | real copy/token routes bound; qualified catalog referenced host files unavailable to the isolated run | Failures remain native provider evidence; Python does not manufacture image bytes or scan filesystem directories. |
| MediaSource local enumeration | VERIFIED_NATIVE | same on all | cna_media_source_get_available_* returned one Local Device snapshot | Repeated snapshots share generation-scoped facades by source index/value identity. |
| MediaSource removable/network coverage | BLOCKED_PLATFORM | same on all | qualified host exposed no Windows Media Connect source | No removable, network, or pseudo-source is fabricated. |
| Song URI construction and metadata | VERIFIED_NATIVE | same on all | cna_song_create_from_uri plus metadata/equality/relationship routes; 20 authored-file cycles | A project-created local file reaches the real Song factory; no SoundEffect substitution is used. |
| MediaPlayer process-global state | VERIFIED_NATIVE | same on all | synchronized generation manager and two-Game scalar/queue tests | Scalar state persists through CNA where XNA defines it; raw handles, registrations, and queued work never cross Game generations. |
| MediaPlayer transport | VERIFIED_NATIVE | same on all | real Play/Pause/Resume/Stop/Move routes; NULL-audio Song playback accepted | Native state and callbacks are observed; audible playback is not claimed for NULL audio. |
| MediaQueue | VERIFIED_NATIVE | same on all | cna_media_player_get_queue and queue routes; 20 generation cycles | One facade per generation, stable Song item/active identity, live replacement, and deterministic stale-generation rejection. |
| MediaPlayer events | VERIFIED_NATIVE | same on all | native subscribe/unsubscribe callbacks; owner-thread dispatcher; 50-delivery stress | Trampolines contain BaseException, copy only generation work, invoke no user code in C, and discard stale events. |
| VisualizationData native route | VERIFIED_NATIVE | same on all | cna_media_player_get_visualization_data; 2056-byte/align-4 structure with offsets 8 and 1032 | The exact route copies into immutable Python tuples; no waveform or spectrum is synthesized. |
| Real visualization spectrum | VERIFIED_NATIVE | same on all | with a generated 440 Hz tone playing, the 256-point spectrum has a non-zero peak and the waveform swings both positive and negative | The historical null backend had no decoded signal, so zeros were all it could report. A real mixer decodes the tone and the visualization follows it. |
| Video metadata object | VERIFIED_NATIVE | same on all | cna_video_create_with_metadata and all public getters | Metadata construction is native and content-owned; it does not pretend that the encoded file has been decoded. |
| Video XNB | VERIFIED_NATIVE | same on all | private VideoReader test covers reader table, cache identity, Unload, stale retained value, and post-create rollback | It reuses ContentManager/XNB/LZX ownership and does not special-case asset names. |
| Video decode | VERIFIED_NATIVE | same on all | a generated clip loaded through ContentManager plays and yields frame textures on both artifacts | The fixture is generated at test time and never committed; the repository ships no encoded media. CNA refuses a video whose declared metadata disagrees with the file, so the clip is produced to match the asset exactly. |
| Video decode on a non-rendering backend | VERIFIED_NATIVE | same on all | the same generated clip decodes on the non-windowed control artifact | Decoding does not depend on the renderer; it was the title-relative path, not the backend, that prevented it before. |
| VideoPlayer control | VERIFIED_NATIVE | same on all | cna_video_player_create/state/property/dispose routes and 20 cycles | Operation-specific disposal behavior preserves cached IsLooped, IsMuted, and Volume; finite out-of-range Volume throws and NaN is retained. |
| VideoPlayer.GetTexture route | VERIFIED_NATIVE | same on all | cna_video_player_get_frame_ext; no frame before playback maps to None | Asking before playback has produced a frame is an ordinary answer of None, matching the canonical implementation rather than faulting. |
| Video frame stable identity/generation | VERIFIED_NATIVE | same on all | cna_video_player_get_frame_ext supplies a monotonic decode generation with the borrowed frame texture | Closed upstream since ABI 0.9. GetTexture returns a real Texture2D that borrows the runtime's frame; it is never owned, never destroyed and never registered per frame, and a use after the borrow ended refuses with the reason it ended. CNA decodes into a single texture rather than alternating two, which is a divergence from XNA's slot model that the generation makes observable rather than hidden. |
| CNB container parse and validation | VERIFIED_NATIVE | same on all | cna_cnb_document_parse/parse_file; tests.test_cnb_format, tests.test_cnb_malformed | Every container invariant is applied before an accessor hands out a byte. Measured on the control artifact: flipping any single byte of a small document either leaves it valid or is refused, and every prefix length is refused; the whole batch also runs in a child process, which exits 0. |
| CNB chunk checksums | VERIFIED_NATIVE | same on all | cna_cnb_crc32c and the stored per-chunk checksum; tests.test_cnb_format.ChecksumTests | CRC-32C reproduces the published check value 0xE3069283 for "123456789", and the hardware and portable paths agree byte for byte on this machine, so a stored checksum does not depend on which path computed it. |
| CNB chunk compression | VERIFIED_NATIVE | same on all | cna_cnb_copy_compressed/copy_decompressed; tests.test_cnb_format.CompressionTests | Zstandard is implemented in both qualified artifacts. LZ4 and Deflate have frozen wire identities and no implementation, and are refused rather than silently stored. Measured limit: a flipped byte in a Zstandard frame is often refused but can still decode to the declared length with different contents, so the exact-size rule bounds allocation and the chunk's own CRC-32C is what catches content corruption. |
| CNB reader, byte writer and container writer | VERIFIED_NATIVE | same on all | the cna_cnb_reader_*, cna_cnb_byte_writer_* and cna_cnb_writer_* families; tests.test_cnb_format | Every primitive round-trips, the encoding is checked against bytes written out by hand rather than only against the reader, and building the same document twice gives byte-identical output. |
| CNB Texture2D/Texture3D/TextureCube codecs | VERIFIED_NATIVE | same on all | cna_cnb_encode_texture2d/cube/3d and their decoders; tests.test_cnb_codecs | Asset data only: no graphics device is involved, so both artifacts run these identically. Six asymmetric cube faces, face-major then mip ordering, and exact level payloads are asserted; the block-rounding rule is computed independently rather than read back. |
| CNB SoundEffect codec | VERIFIED_NATIVE | same on all | cna_cnb_encode_sound_effect/decode_sound_effect; tests.test_cnb_codecs | Asset data only: no audio device is involved. The AUDH header's six fields are decoded by hand from the chunk as well as through the codec, so a sample-rate/channel swap is visible. |
| CNB SpriteFont codec | VERIFIED_NATIVE | same on all | cna_cnb_encode_sprite_font/decode_sprite_font; tests.test_cnb_codecs | All five font chunks and all four strides are asserted against the glyph count. The atlas travels inside the file and comes back independently owned. MeasureString is not used as an oracle; the encoded data graph is. |
| CNB Song and Video codecs | VERIFIED_NATIVE | same on all | cna_cnb_encode_song/encode_video and their decoders; tests.test_cnb_codecs | Both schemas store metadata plus an external stream reference rather than embedded media, which is asserted: the stream name appears in XREF, not in a payload chunk. No playback is involved and none is claimed. |
| CNB Curve codec | VERIFIED_NATIVE | same on all | cna_cnb_encode_curve/decode_curve over the eleven-route curve.h slice; tests.test_cnb_codecs | The codec speaks in native Curve handles, so a native curve is built from the managed keys and destroyed before the call returns. What comes back is an ordinary managed Microsoft.Xna.Framework.Curve with nothing to close. |
| CNB AnimationClip codec | VERIFIED_NATIVE | same on all | cna_cnb_encode_animation_clip/decode_animation_clip; tests.test_cnb_codecs | Non-symmetric transforms throughout, so a translation/scale swap or a quaternion reorder is a different value. The target space is content and is asserted for both identities rather than defaulted. |
| CNB Model graph encode and decode | VERIFIED_NATIVE | same on all | cna_cnb_encode_model/decode_model and the 58-route model family; tests.test_cnb_model | One deliberately asymmetric fixture: three chained bones, two parts with different vertex and index byte lengths, a mesh naming its parts out of index order, a full material with three texture names and all three per-slot arrays, a skeleton with a root prefix, two morph targets on different delta streams, a cubic-spline weight key, an animation with two tracks, and a light. The whole graph survives byte-identical encoding. |
| CNB model built directly from a .cnj | VERIFIED_NATIVE | same on all | cna_cnb_build_model_from_cnj and cna_cnb_model_from_cnj_take_model; tests.test_cnb_pipeline | take_model transfers ownership exactly once and a second call raises rather than producing a second owner of the same native model; the taken model outlives the result it came from. Measured difference from compile_cnj: this route does not list the .cnj among its absorbed files, and an empty model document yields one synthesized Root bone. |
| CNJ compilation | VERIFIED_NATIVE | same on all | cna_cnb_compile_cnj; tests.test_cnb_pipeline, tests.test_cnb_tool_crosscheck | Compiling the same document twice gives identical bytes, and so does CNA's own cna_tool_cnj_to_cnb in a separate OS process -- two processes share no allocator state or warm heap, which makes that the strongest determinism evidence available. |
| CNJ sidecar containment | VERIFIED_NATIVE | same on all | ResolveCnjSourceFileSafely through cna_cnb_compile_cnj; tests.test_cnb_pipeline.SidecarContainmentTests | A parent traversal, a traversal to a file that exists, an absolute path, a missing sidecar and a truncated sidecar are each refused. Measured against cnb.h's prose: content_root is a containment boundary, not a resolution base -- a sidecar always resolves against the document's own directory. |
| CNB source importers: image, DDS, WAV | VERIFIED_NATIVE | same on all | cna_cnb_import_image_as_texture2d, import/decode_dds_as_texture_cube, import/decode_wav_as_sound_effect; tests.test_cnb_pipeline | Each is asserted against pixels and samples the test generated, not against a file size. The colour key is applied only when asked for. 8-bit PCM is widened exactly and 24-bit is refused by name rather than truncated. |
| CNB loader registry: registration and resolution | VERIFIED_NATIVE | same on all | the cna_cnb_loader_registry_* family; tests.test_cnb_pipeline.LoaderRegistryTests | register_builtins installs Curve and AnimationClip and no other built-in type, which is asserted for all ten. Resolution proves identity as well as number for a custom type; measured, the writer refuses to produce a colliding file at all, so there is no path through this API to one its own loader would have to refuse. |
| CNB loader invocation from Python | VERIFIED_NATIVE | same on all | cna_cnb_loader_invoke with cna_content_manager_create; tests.test_cnb_loaders | Measured on the control artifact inside one frame of a real Game. A Python loader is registered, resolved for its document and invoked, and the very object it returned comes back by identity. An exception raised inside a loader arrives as itself after CNA has returned through its own frames. A built-in loader's C++ object is reported as unsupported rather than handed across. |
| CNB loader object generality | LANGUAGE_MAPPING_LIMITATION | same on all | cna_cnb_loader_invoke's void* out_object; cnb.h's own note that a C++ loader answers CNA_RESULT_NOT_SUPPORTED | A Python loader's object crosses as an opaque token this package resolves back, which works only within this process and only for loaders registered from Python. CNA's built-in loaders construct C++ objects that cannot cross the C boundary at all. The API reports that rather than faking a generality the ABI does not have. |
| Native ContentManager beyond loader invocation | DELIBERATE_OUT_OF_SCOPE | same on all | content.h has roughly 92 routes; exactly cna_content_manager_create and cna_content_manager_destroy are bound | Opened only as far as cna_cnb_loader_invoke requires, which is measured: passing no manager is refused. It is a separate cache domain from Microsoft.Xna.Framework.Content.ContentManager, asserted rather than described, and no load, cache, manifest or root-directory route is bound. |
| CNB Effect asset schema | BLOCKED_UPSTREAM | same on all | cnb.h reserves CNA_CNB_ASSET_TYPE_EFFECT with no schema; cna_cnb_compile_cnj refuses "Effect" by name | By design rather than by omission: CNA has many renderers, so a .cnb carrying one API's shader bytecode would be useless on the others. Nothing is fabricated; a caller asking for one is refused with the reason. |
| CNB checked integer arithmetic routes | NOT_USEFUL_FOR_PYTHON | same on all | cna_cnb_checked_add and cna_cnb_checked_multiply are the two cnb.h routes not imported | They refuse instead of wrapping around when two file-declared 64-bit values are combined. Python integers are unbounded, so the same computation is already exact; a call would turn an exact answer into a narrower one. The bounds checks that matter happen where a value has to fit a native width, and name the value rather than truncating it. |
| Engine layer availability | VERIFIED_NATIVE | control: absent (version 0); gpu: present (version 2) | cna_engine_layer_get_version; cna_engine_layer_copy_version_string; tests/test_engine_compute.py EngineIdentityTests | The two artifacts genuinely differ and that is the point of the row. Both export all 857 engine symbols; only the GPU artifact has an engine layer behind them. cna.extensions.engine.is_available() reads the version route, never the symbol table or the renderer's name. |
| Engine absence reported as a build fact | VERIFIED_NATIVE | control: EngineUnavailableError raised; gpu: not applicable | tests/test_engine_compute.py EngineAbsenceTests | Constructing an engine object on a build with no engine layer raises EngineUnavailableError, not EngineUnsupportedError. Only the control artifact can prove this side of the distinction. |
| Compute memory-barrier mask | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_graphics_memory_barrier_has; seven containment cases cross-checked against Python's own bit arithmetic | The nine named bits are distinct powers of two and fold exactly to ALL. CNA's containment answer is compared with Python's rather than trusted. |
| Storage buffer, byte and typed | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_storage_buffer_* (10 routes); tests/test_engine_compute.py StorageBufferTests | Upload and readback preserve bytes; a typed buffer reports both its element count and element size, a byte-sized one reports zero for both, and an element upload of the wrong shape is refused before CNA. |
| Compute shader dispatch | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified on Mesa GL ES 3.2 | cna_compute_shader_* (12 routes); tests/test_engine_compute.py ComputeShaderTests | Evidence is buffer content Python predicts from the shader's own arithmetic, not a result code: uniforms reach the program, a changed uniform changes every element, and a second bound buffer is genuinely read. Two project-authored GLSL ES 3.10 programs. |
| Compute shader image binding | BLOCKED_RENDERER | control: unavailable (no engine layer); gpu: unsupported | cna_compute_shader_is_image_binding_supported answers false on the GPU artifact | A capability separate from compute itself: this renderer compiles and dispatches compute programs and still cannot bind a texture as a read/write image. bind_image refuses by name rather than failing inside CNA. |
| Compute shader compile diagnostics | BLOCKED_UPSTREAM | control: unavailable (no engine layer); gpu: measured | ENGINE-001 in docs/engine-upstream-findings.md; six bad sources, all CNA_RESULT_INTERNAL with no handle | engine_layer.h documents creation as succeeding for source that does not compile, with the failure read back through is_valid. It fails instead, and reports a caller's bad shader in the internal category. The binding raises ComputeShaderCompileError, keeps result 12 verbatim, and carries the compiler log. |
| GPU timer query | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_gpu_timer_* (11 routes); tests/test_engine_compute.py GpuTimerTests | Timing is nondeterministic, so no duration is asserted. What is asserted is the state machine: closed before, open inside the measurement, closed after, a result that arrives on polling, a sample count that advances by exactly one, and a finite non-negative duration. |
| Engine render-target pool | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_render_target_pool_* (6 routes); tests/test_engine_postprocess.py RenderTargetPoolTests | The same shape and slot reuses one target and a new slot allocates another; disposing a view leaves the pooled storage; reset is refused while a view is out and succeeds after. A pooled view is a working RenderTarget2D: it binds, clears and reads back. |
| Engine render-target scope | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_scoped_render_target_* (3 routes); tests/test_engine_postprocess.py RenderTargetScopeTests | Proven by pixels rather than by a flag: a clear after the scope closes lands on the outer target and not on the inner one. Nested scopes refuse an out-of-order close, and neither the stack nor the binding changes when they do. |
| Engine shader-effect factory | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified on Mesa GL ES 3.2 | cna_shader_effect_factory_* (6 routes); tests/test_engine_postprocess.py ShaderEffectFactoryTests | A second acquire of one name compiles nothing, measured by compile_count rather than assumed. The effect comes back as a strict Effect view with parameters and techniques; clear is refused while a view is out, and clearing does not reset the compile count. |
| Engine full-screen draw | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_fullscreen_pass_* (4 routes); tests/test_engine_postprocess.py FullscreenPassTests | A copy reproduces all sixteen pixels of an asymmetric 4x4 source exactly, so a transpose, flip, swizzle or offset would fail rather than pass. |
| Post-process pass and blit | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_post_process_pass_*, cna_blit_pass_create, cna_post_process_context_init; tests/test_engine_postprocess.py PostProcessPassTests | A blit reproduces its source exactly. An effect pass halves red and leaves green and blue untouched, which is the shader's own arithmetic computed independently in Python. A closed pass refuses before reaching CNA. |
| Post-process chain ordering | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_post_process_chain_* (12 routes); tests/test_engine_postprocess.py PostProcessChainTests | Order is proven with two passes that do not commute: (r/2)-64 gives 36 and (r-64)/2 gives 68 from the same source. A chain with no source or a non-positive size is refused. Two blits still reproduce the source exactly through the pooled intermediates. |
| Post-process effect handle lifetime | BLOCKED_UPSTREAM | control: unavailable (no engine layer); gpu: measured and worked around | ENGINE-002 in docs/engine-upstream-findings.md | cna_post_process_effect_pass_get_effect returns a new counted handle per call, which the header says must not be destroyed. Not destroying it stops the factory and then the game being destroyed. The binding releases the view immediately, so reading the property any number of times leaks nothing. |
| GPU timer first sample | BLOCKED_UPSTREAM | control: unavailable (no engine layer); gpu: measured | ENGINE-003; the first collected duration is exactly 4294.967295 ms on every run | An unsigned 32-bit underflow, not a duration. Reported verbatim rather than discarded, because discarding it would hide the defect and disagree with sample_count. Both the standalone timer and the chain's timings show it, and both are pinned by tests that fail when CNA fixes it. |
| Post-process chain GPU timing | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified across frames | cna_post_process_chain_get_pass_timing*; ENGINE-004 for the flag; eight frames, seven samples | Timing works: one sample per frame after the first, around 0.003 ms for a 4x4 blit. The flag is not observable until the chain has applied once, which is ENGINE-004 -- the header's documented read-back reports no timing on a device that is about to time. Measured across frames, because a query opened in one frame is collected in a later one. |
| Engine light values | VERIFIED_NATIVE | control: verified (no engine layer needed); gpu: verified | cna_directional_light_ext_init and its four siblings; tests/test_engine_shadows.py LightValueTests | The one piece of engine surface that answers on a build with no engine layer, which engine_layer.h promises and this measures on the artifact that has none. No default is written down in Python: each value type reads CNA's through its _init route, so a changed default cannot drift silently. |
| Shadow quality presets | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_shadow_map_size_for_quality, cna_shadow_map_filter_radius_for_quality, cna_cube_shadow_map_size_for_quality | Sizes are powers of two, rise with quality, and a cube face is never larger than a flat map -- six faces cost six times one. The numbers are read from CNA rather than written down. |
| Shadow matrix helpers | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_shadow_map_compute_light_view/projection, the four cascade helpers, the spot and cube helpers; tests/test_engine_shadows.py ShadowMatrixTests and CascadeMathTests | Compared against tests/engine_oracles.py, which computes the same matrices from the stated algorithm without calling CNA. The oracles are themselves gated by tests/test_engine_oracles.py against values worked by hand. Fixtures are asymmetric: a slanted light, a non-cubic off-origin box and non-round split planes, so a transpose or an axis swap fails. |
| Directional shadow map | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_shadow_map_* (20 routes); tests/test_engine_shadows.py ShadowMapTests | An empty map reads as the far plane -- 1.0 in a Single target -- which is what keeps an unwritten texel from being an occluder at zero distance. The transform the pass computes is exactly the two pure helpers composed. Casting and sampling are asked separately. |
| Cascaded shadow map | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_cascaded_shadow_map_* (24 routes); tests/test_engine_shadows.py CascadedShadowMapTests | Three cascades occupy one atlas three cascades wide. Splits increase, end on the far plane, match the pure helper for the same camera range, and four cascades give four different transforms. Deeper points select later cascades. |
| Spot and cube shadow maps | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_spot_shadow_map_* and cna_cube_shadow_map_* (33 routes); tests/test_engine_shadows.py SpotAndCubeShadowMapTests | A spot map records the light it cast from and its transform is the two pure helpers composed. A cube map really is a TextureCube -- measured: it answers cna_texturecube_get_info and is released with cna_texturecube_destroy, unlike the other three, which are render targets. Its six face views look six different ways, which is the test a face-order swap fails. |
| Shadow receiver effect state | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_effect_*_ext (14 routes); tests/test_engine_shadows.py ShadowReceiverTests | IShadowReceiverEXT is an interface C cannot implement, so what crosses is its operations. They are wrapped rather than added to Effect, because CNA-only state must not appear on a strict XNA type. Cascade state and punctual lights cross whole, with four distinct transforms and four distinct splits so a state that copied one entry over all of them would show. |
| Engine counted-view lifetime | BLOCKED_UPSTREAM | control: unavailable (no engine layer); gpu: measured and worked around | ENGINE-002 in docs/engine-upstream-findings.md | Every engine getter that answers with a handle returns a fresh counted view that must be released. One route documents the opposite and two document nothing. Objects that own a useful view hand it out once and dispose it on close; the rest release immediately. A test closes a map after reading its texture and its caster effect and proves both were released. |
| PBR material value | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_pbr_material_ext_* and cna_pbr_material_ext_init; tests/test_engine_pbr.py PbrMaterialValueTests | Twenty-seven fields, every default read from CNA rather than written down. Each field is changed alone and CNA's own equality confirms it reached the native value. The seven texture slots are keyed by slot rather than by structure position, because CNA numbers emissive before occlusion and declares them the other way round. |
| PBR material extensions | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_pbr_material_extensions_* (61 routes); tests/test_engine_pbr.py PbrMaterialExtensionsTests | Clearcoat, sheen, transmission, volume, iridescence and subsurface: twelve scalars, three colours and nine texture slots all round-trip. The is_* flags are CNA's derived answers, not stored state -- setting a transmission factor turns transmission on and zeroing it returns the object to neutral. |
| glTF material bridge | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_gltf_material_* (6 routes); tests/test_engine_pbr.py GltfBridgeTests | Every core factor reaches the material, including each slot's own coordinate set and transform. The bridge quantises glTF's linear float base colour into the material's byte colour, which the test asserts as a quantisation rather than as an identity. |
| Thin-film iridescence | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_thin_film_iridescence_evaluate and _copy_glsl | The term varies with film thickness and separates the channels, which is what makes it iridescence rather than a tint; a film matching the surrounding medium collapses toward its base reflectance. CNA also publishes the GLSL, so a custom shader and the CPU evaluation agree by construction. |
| PBR effect material state | BLOCKED_UPSTREAM | control: unavailable (no engine layer); gpu: measured and worked around | ENGINE-005 in docs/engine-upstream-findings.md | cna_pbr_effect_apply_material is documented to carry every field and carries every scalar; not one of the seven texture slots crosses, confirmed by an independent route in another header. The public setter assigns the slots itself so a material really is applied, and a test drives the raw route so the defect stays visible. |
| PBR effect construction | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_pbr_effect_create and cna_skinned_pbr_effect_create, the effects.h dependency slice; tests/test_engine_pbr.py PbrEffectTests | engine_layer.h has no route that creates a PbrEffect, so without this two-route slice every PBR route in it would be unreachable. The effect comes back as a strict Effect with its parameters and techniques, and applying a material's state really does change the device's blend and cull state. |
| Particle simulation | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified on the CPU path | cna_particle_system_* (24 routes); tests/test_engine_scene.py ParticleSystemTests and ParticleValueTests | The strongest evidence available for something usually called random: CNA's draw is a hash of a seed, so a system is reproducible and the tests predict it. Every particle a forced-CPU system produced is compared with cna_particle_system_step applied by hand from the state one update earlier. The live count is emission rate times lifetime, capped by the pool, and the cap is reported separately. |
| Particle GPU simulation | BLOCKED_FIXTURE | control: unavailable (no engine layer); gpu: not separated from the CPU path | cna_particle_system_uses_compute reports which path ran | The CPU path is what makes a simulation inspectable -- cna_particle_system_copy_particles_ext reads the pool back -- so that is the path the step-by-step comparison runs on. Whether the compute path produces the same particles is a separate claim this artifact has no way to read back, and it is not made. |
| Soft particle depth fade | BLOCKED_FIXTURE | control: unavailable (no engine layer); gpu: state verified, fade not | cna_particle_system_set_depth_input_ext and the softness pair round-trip | Setting a depth input and a softness is verified; that particles actually fade against a depth field is not. Proving it needs a rendered comparison between a system with and without the input, against geometry at a known depth, and no such fixture exists yet. Reported rather than claimed from the fact that the setters work. |
| Decal pass state | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_decal_pass_* (12 routes); tests/test_engine_scene.py DecalPassTests | Opacity, tint and the maximum slope round-trip, and the unit-box containment test is cross-checked against an independent implementation. Projected decal pixels are not asserted: that needs a prepass rendered from known geometry, which is the fixture the row above also wants. |
| Depth/normal prepass | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified, one pass with two targets | cna_depth_normal_prepass_* (27 routes); tests/test_engine_scene.py DepthNormalPrepassTests | The renderer writes depth and normals in one pass, so pass_count is one and uses_multiple_render_targets is true -- the two answers are asserted to agree rather than being read separately. Depth and normals are distinct targets of the requested shape, an empty packed prepass reads back as the far plane, and a resize disposes the old views rather than leaving them outstanding. |
| Prepass depth encoding | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_depth_normal_prepass_pack_depth/unpack_depth and the velocity pair | Packing is compared against an independent implementation computed in single precision, because that is where the answer comes from: depth times 2**24 exceeds a float's mantissa, so the top channel is genuinely zero. A thousand depths round-trip to within 1e-5, and a far-plane depth does not wrap to the nearest surface. A velocity texel's alpha marks presence, so an unwritten texel is absent rather than zero. |
| Sorted transparency | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_transparent_draw_list_* (9 routes); tests/test_engine_scene.py TransparentDrawListTests | Entries submitted near-first are replayed far-first, and moving the camera behind them reverses the order, so submission order is not what the test would accept. The callbacks are rooted by the list: four callables nothing else refers to survive a gc.collect() and are all invoked. A Python exception inside one surfaces after the native call rather than unwinding through C, and the list stays usable. |
| Order-independent transparency | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_weighted_blended_transparency_* (13 routes); tests/test_engine_scene.py | Two distinct targets of the requested shape, an accumulation bracket that opens and closes, and a resolve. The weight function is cross-checked against an independent implementation; its lower clamp is measured to be unreachable, because the depth is normalised first, which is worth knowing rather than assuming it binds. |
| Render pipeline settings | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_render_pipeline_settings_ext_* (4 routes); tests/test_engine_pipeline.py RenderPipelineSettingsTests | Forty-seven settings, projected from the measured layout with CNA's own per-field documentation rather than transcribed, and a test asserts the property set is exactly the structure's fields minus its versioning header. Every field round-trips through its own type. Normalising applies the floors the engine would -- gamma, exposure, bloom intensity, SSR edge fade -- so a caller sees the correction before the picture does. |
| Render pipeline frame | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_render_pipeline_* (33 routes); tests/test_engine_pipeline.py RenderPipelineTests | A frame is bracketed and what it did is read back from CNA rather than inferred from the settings that asked: turning on bloom, FXAA and HDR runs strictly more passes and brings the scene target into use. The scene target is one counted view disposed with the pipeline, and releasing device resources disposes it too rather than leaving it pointing at freed storage. |
| Render pipeline callbacks | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_render_pipeline_set_shadow_scene and set_transparent_scene; tests/test_engine_pipeline.py | Both callbacks are rooted by the pipeline, replacing one drops the previous root, and a Python exception inside one surfaces after the frame rather than unwinding through C -- with the pipeline still usable afterwards, which the test proves by drawing another frame. |
| Engine float constants | VERIFIED_NATIVE | control: verified; gpu: verified | tools/generate_engine_abi.py emits float constants at single precision; tools/audit_cna_abi.py compares them at that width | A constant declared 0.01F in a header is 0.009999999776482582, and a Python 0.01 is a different, larger number. Emitting the double would make a caller comparing a value CNA produced against the constant find it one unit in the last place too small -- which is exactly how the render pipeline's gamma floor was caught. |
| Post-process pass state | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_bloom_pass_*, cna_tonemap_pass_*, cna_fxaa_pass_*, cna_film_grain_pass_*, cna_chromatic_aberration_pass_*, cna_lens_flare_pass_*, cna_ascii_pass_*; tests/test_engine_passes.py PassStateTests | Every pass's own state round-trips, and eight passes answer to eight different names rather than one -- which a pass constructed through the wrong route would fail. The properties are built by a factory that takes both route names as literals, so the reachability gate can still see them. |
| Post-process CPU twins | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_bloom_pass_extract_channel, cna_tonemap_pass_tonemap_channel, the four HDR helpers, the two quality helpers | Bloom's extraction is a soft knee rather than a cutoff -- a texel exactly at the threshold contributes a quarter of itself, so bloom does not pop on and off as a highlight crosses it -- and it is compared against an independent implementation of that. Five tonemap curves give five different answers for one input and each is monotonic; PQ encode and decode are inverses across four decades; Rec.2020 keeps white and desaturates a Rec.709 primary. |
| Tonemap rendered output | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | tests/test_engine_passes.py TonemapPixelTests | A pass that stored its settings and ignored them would pass every round trip, so one is checked against pixels: a Reinhard tonemap over a known colour lands within two levels of what the CPU twin predicts for the same three channels, and darkens them. |
| Colour grading and cube LUTs | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified; parsing needs no device | cna_color_grade_pass_* and cna_cube_lut_* (21 routes); tests/test_engine_passes.py | A .cube document is a file format, so parsing and inspecting one needs no device and the tests run it on any build with an engine layer. The eight corners of a 2x2x2 identity table are read back individually, which is what an index order read backwards would fail. Three malformed documents are refused. |
| HDR display output | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: state and encoding verified | cna_hdr_display_output_* (15 routes) | Three colour spaces encode one scene colour three different ways, roll-off never exceeds the peak and is monotonic, and the defaults are the header's own constants at single precision. Whether a display actually presents in a wide space is the renderer's answer and is reported, not assumed. |
| Auto exposure adaptation | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_auto_exposure_ext_* (13 routes); tests/test_engine_passes.py AutoExposureTests | Adaptation is time-based, so the direction is what is predicted rather than a value: from the same starting exposure, a white scene settles below a black one, and a range of 0.5 to 0.75 holds whatever the scene. The exposure then reaches a render pipeline's settings value. |
| ASCII pass configuration | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_ascii_pass_get_effect plus the six-route graphics_ext.h slice | Nothing in engine_layer.h can read or write the pass's cell size or quantise mode, so without the slice the pass would have a getter returning something unusable. The effect is one counted borrow, handed out once and released when the pass closes. |
| Spatial upscaling | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_spatial_upscale_pass_* (8 routes) | State round-trips, an upscale to the same size is reported as the identity so a caller can skip it, and a draw onto a target cleared to black leaves it not black -- which is weaker than a predicted filter kernel and is what a scaling filter admits to. |
| Screen-space effect state | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_ssao_pass_*, cna_ssr_pass_*, cna_motion_blur_pass_*, cna_depth_of_field_pass_*, cna_contact_shadow_pass_* (60 routes); tests/test_engine_atmosphere.py | Nine passes answer to nine different names. Three measured bounds are now documented at the properties rather than assumed: SSR's roughness blur clamps to a quarter and its edge fade to a half, depth of field's maximum radius clamps to a quarter and is therefore a fraction of the screen rather than a pixel count, and several setters ignore a non-positive value instead of refusing it -- so the previous value survives. |
| SSR step-count bounds | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: measured | cna_ssr_pass_set_step_count stores what it is given; the trace clamps | The header declares a minimum and a maximum the trace takes 'whatever it is told', and the setter does not clamp: setting one reads back one, and the clamp happens where the ray march runs. The property says so, and the test asserts what CNA does rather than what the constants might suggest. |
| SSAO sample kernel | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_ssao_pass_copy_kernel; tests/test_engine_atmosphere.py | A kernel is the one part of SSAO checkable without rendering, and every offset is inside the unit hemisphere, non-zero, and distinct from the others. Its length is CNA's rather than the sample count -- asking for the sample count answers BUFFER_TOO_SMALL, which is the right refusal and the reason the size is asked for first. |
| Atmospheric model | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_atmospheric_sky_radiance, the aerial-perspective pair, cna_height_fog_pass_optical_depth | A horizon ray crosses more air than a vertical one; more air transmits less and what survives reddens, because blue scatters most; fog accumulates with distance and density and thins with height; the sky is brighter toward the sun and changes with turbidity. Each is a physical prediction rather than a stored value, and the turbidity comparison is relative because a zenith radiance is a number near 1e-5. |
| Sky and skybox rendering | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna_atmospheric_sky_* and cna_skybox_* (26 routes) | The sky draws over a target cleared to black and leaves it not black. A skybox borrows its cube: closing the skybox leaves the cube usable, which the test asserts after the with-block. CNA's ownership-transferring setter is deliberately not used, for the same reason as the other transfers. |
| Engine counted views, systematically | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | _cna_native.engine_support.borrowed_view; every handle-returning getter in the package | Rather than a table of which routes hand out a fresh view -- which would go stale the day CNA changes one -- every getter compares the handle it gets against the handle this binding supplied and releases only a different one. That is what makes reading a property any number of times leak nothing while never destroying the caller's own object. |
| Clustered light set and its bounding spheres | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | tests/test_engine_clustered.py::ClusteredLightSetTests | A CPU collection of light values. Every bounding sphere is compared with one tests/engine_oracles.py derives from the cone geometry, including the two-case split a spot light uses: a cone narrower than 45 degrees is bounded by the sphere through its apex and rim, which is not centred on the light. The 256-light ceiling is a measured constant and the refusal past it is asserted. |
| Clustered light grid: logarithmic depth slicing | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | tests/test_engine_clustered.py::ClusteredLightGridTests; tests/engine_oracles.py slice_distance, cluster_bounds | Every slice boundary, every cluster index and every one of the 30 cluster boxes of a 3x2x5 grid is compared with an independent computation, including a 4x4 matrix inverse written out rather than taken from Matrix.Invert. The oracle computes the exponential at single precision, because the float and double results differ in the last bits and that decides which cluster a light is in. |
| Cluster boxes are conservative, not a partition | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | tests/test_engine_clustered.py::ClusteredLightGridTests::test_the_slices_meet_exactly_in_depth_and_the_tiles_overlap_across_it | Measured: neighbouring clusters meet exactly in depth and overlap across the screen, because an axis-aligned box around a frustum slab is as wide as that slab's far face. A light is therefore assigned to clusters it only nearly touches and never missed from one it reaches. Written down because the obvious expectation -- a clean tiling -- is false, and a test asserting it would have been asserting a defect. |
| Clustered light assignment, CPU and GPU | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | tests/test_engine_clustered.py::ClusteredLightAssignmentTests, ::ClusteredLightComputeTests | Three implementations are compared entry for entry: CNA's CPU sort, CNA's compute shader, and a whole reimplementation of the sort in tests/engine_oracles.py. The two CNA paths are designed to agree exactly rather than plausibly -- the shader is handed the grid's own inverse projection so the two never differ in their last bits -- and the test asserts equality rather than similarity. Also checked under a view matrix that is not the identity, which is what a missing world-to-view transform would hide. |
| Clustered shadow budget | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | tests/test_engine_clustered.py::ClusteredShadowPolicyTests; tests/engine_oracles.py shadow_policy_score | Every score is compared with Rec. 709 luminance times intensity times the windowed falloff, computed independently. Two setters ignore an out-of-range value instead of refusing it -- a negative budget and a hysteresis below one leave the old value in place -- which is asserted rather than described. The hysteresis bonus is applied to the incumbent's score rather than as a rule in the comparison, and the test shows a weaker incumbent holding its slot against a challenger inside the bonus. |
| Clustered light buffer and its published GLSL | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | tests/test_engine_clustered.py::ClusteredLightBufferTests, ::PublishedGlslTests | A mismatched set, grid and assignment is refused rather than uploaded, which is asserted with an assignment built for a different grid and one built for fewer lights. Binding needs an effect compiled from source; an Effect built from XNA bytecode is refused, because the buffer sets uniforms by name and bytecode has none. The uniform names the published GLSL declares are checked against the ones bind sets, which is the contract's two halves. |
| Clustered forward effect material bounds | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | tests/test_engine_clustered.py::ClusteredForwardEffectTests | Four different rules, measured and asserted separately: base colour and metallic clamp to 0..1, roughness clamps to 0.04..1 (a perfectly smooth microfacet distribution divides by zero), ambient floors each channel at zero without a ceiling, and index of refraction *ignores* a value below one rather than clamping it. The roughness floor is asserted at the single-precision float CNA holds, not at the decimal 0.04. |
| Clustered forward effect material extensions view | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | cna.extensions.engine.ClusteredForwardEffect.material_extensions | The getter hands out a counted view onto the effect's own extensions and the setter *replaces* the object behind it, so a view taken before an assignment looks at a set that no longer exists. The cached view is dropped on assignment; without that, reading the property back after setting a clearcoat factor answered zero. Found by a test that assigned and read back rather than by inspection. |
| Area lights: quad, coverage and BRDF | VERIFIED_NATIVE | control: partly (value defaults and is_valid only); gpu: verified | tests/test_engine_clustered.py::AreaLightShadingTests, ::BrdfEvaluationTests | The rectangle quad is compared corner for corner with an independent construction, and the disc quad is checked against the identity its axis scale exists for: pi*a*b equals the scaled rectangle's area. A tube's quad is billboarded, so it is checked by the property that it turns with the surface while its axis does not. Coverage is measured to emit from the +z face of the default light, to be zero for a surface in the light's own plane, and to rise as the lobe narrows. The BRDF is a Monte Carlo integral, so it is checked against what the integral must be -- a fraction in 0..1, a unit average direction, monotone in roughness, convergent in sample count and deterministic -- rather than against a transcription of the same importance sampling. |
| Which clustered value routes answer without an engine layer | VERIFIED_NATIVE | control: verified; gpu: verified | tests/test_engine_clustered.py::ClusteredAbsenceTests | Measured and matching what engine_layer.h documents for each route: both _init routes fill a value with its canonical defaults on any build, AreaLight.is_valid answers on any build ('SUCCESS in every build'), and ClusteredLight.is_usable does not, because that rule belongs to the light set rather than to the value. Nine other routes are asserted to report the layer as unavailable rather than answering with a plausible number. |
| The clustered constructors take a device, not a game | VERIFIED_NATIVE | control: unavailable (no engine layer); gpu: verified | docs/engine-upstream-findings.md ENGINE-006; tests/test_engine_clustered.py::ClusteredConstructorTests | engine_layer.h documents the first parameter of four constructors as 'the owning game'. The C layer resolves it as a graphics device and refuses a game handle with CNA_RESULT_INVALID_HANDLE. Every class in the package takes a GraphicsDevice, so the difference is invisible from Python; the raw route is driven with both handles so the pin fails the day either side changes. |
