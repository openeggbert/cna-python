{
 "admitted": {
  "cna_basic_effect_get_diffuse_color": "A stock-effect property getter. Its name is assembled from literals by a property factory that is passed to _stock_property by reference, so the assembly is statically real but not statically followable here. Reading the property calls it; the suite only writes these.",
  "cna_basic_effect_get_emissive_color": "A stock-effect property getter. Its name is assembled from literals by a property factory that is passed to _stock_property by reference, so the assembly is statically real but not statically followable here. Reading the property calls it; the suite only writes these.",
  "cna_basic_effect_get_specular_color": "A stock-effect property getter. Its name is assembled from literals by a property factory that is passed to _stock_property by reference, so the assembly is statically real but not statically followable here. Reading the property calls it; the suite only writes these.",
  "cna_basic_effect_get_specular_power": "A stock-effect property getter. Its name is assembled from literals by a property factory that is passed to _stock_property by reference, so the assembly is statically real but not statically followable here. Reading the property calls it; the suite only writes these.",
  "cna_alpha_test_effect_get_diffuse_color": "A stock-effect property getter. Its name is assembled from literals by a property factory that is passed to _stock_property by reference, so the assembly is statically real but not statically followable here. Reading the property calls it; the suite only writes these.",
  "cna_alpha_test_effect_get_alpha": "A stock-effect property getter. Its name is assembled from literals by a property factory that is passed to _stock_property by reference, so the assembly is statically real but not statically followable here. Reading the property calls it; the suite only writes these.",
  "cna_alpha_test_effect_get_vertex_color_enabled": "A stock-effect property getter. Its name is assembled from literals by a property factory that is passed to _stock_property by reference, so the assembly is statically real but not statically followable here. Reading the property calls it; the suite only writes these.",
  "cna_dual_texture_effect_get_diffuse_color": "A stock-effect property getter. Its name is assembled from literals by a property factory that is passed to _stock_property by reference, so the assembly is statically real but not statically followable here. Reading the property calls it; the suite only writes these.",
  "cna_dual_texture_effect_get_alpha": "A stock-effect property getter. Its name is assembled from literals by a property factory that is passed to _stock_property by reference, so the assembly is statically real but not statically followable here. Reading the property calls it; the suite only writes these.",
  "cna_environment_map_effect_get_diffuse_color": "A stock-effect property getter. Its name is assembled from literals by a property factory that is passed to _stock_property by reference, so the assembly is statically real but not statically followable here. Reading the property calls it; the suite only writes these.",
  "cna_environment_map_effect_get_emissive_color": "A stock-effect property getter. Its name is assembled from literals by a property factory that is passed to _stock_property by reference, so the assembly is statically real but not statically followable here. Reading the property calls it; the suite only writes these.",
  "cna_environment_map_effect_get_alpha": "A stock-effect property getter. Its name is assembled from literals by a property factory that is passed to _stock_property by reference, so the assembly is statically real but not statically followable here. Reading the property calls it; the suite only writes these.",
  "cna_skinned_effect_get_diffuse_color": "A stock-effect property getter. Its name is assembled from literals by a property factory that is passed to _stock_property by reference, so the assembly is statically real but not statically followable here. Reading the property calls it; the suite only writes these.",
  "cna_skinned_effect_get_emissive_color": "A stock-effect property getter. Its name is assembled from literals by a property factory that is passed to _stock_property by reference, so the assembly is statically real but not statically followable here. Reading the property calls it; the suite only writes these.",
  "cna_skinned_effect_get_specular_color": "A stock-effect property getter. Its name is assembled from literals by a property factory that is passed to _stock_property by reference, so the assembly is statically real but not statically followable here. Reading the property calls it; the suite only writes these.",
  "cna_skinned_effect_get_specular_power": "A stock-effect property getter. Its name is assembled from literals by a property factory that is passed to _stock_property by reference, so the assembly is statically real but not statically followable here. Reading the property calls it; the suite only writes these.",
  "cna_skinned_effect_get_alpha": "A stock-effect property getter. Its name is assembled from literals by a property factory that is passed to _stock_property by reference, so the assembly is statically real but not statically followable here. Reading the property calls it; the suite only writes these.",
  "cna_skinned_effect_get_prefer_per_pixel_lighting": "A stock-effect property getter. Its name is assembled from literals by a property factory that is passed to _stock_property by reference, so the assembly is statically real but not statically followable here. Reading the property calls it; the suite only writes these.",
  "cna_skinned_effect_get_texture": "A stock-effect property getter. Its name is assembled from literals by a property factory that is passed to _stock_property by reference, so the assembly is statically real but not statically followable here. Reading the property calls it; the suite only writes these.",
  "cna_album_get_name_size": "A catalog text or image accessor whose name is built in two steps from a class's literal _stem. It is reached whenever a catalog item exists; this host's music catalog is empty and picture bytes are platform-pending, so no test can reach one.",
  "cna_album_copy_name": "A catalog text or image accessor whose name is built in two steps from a class's literal _stem. It is reached whenever a catalog item exists; this host's music catalog is empty and picture bytes are platform-pending, so no test can reach one.",
  "cna_album_get_art_size": "A catalog text or image accessor whose name is built in two steps from a class's literal _stem. It is reached whenever a catalog item exists; this host's music catalog is empty and picture bytes are platform-pending, so no test can reach one.",
  "cna_album_copy_art": "A catalog text or image accessor whose name is built in two steps from a class's literal _stem. It is reached whenever a catalog item exists; this host's music catalog is empty and picture bytes are platform-pending, so no test can reach one.",
  "cna_album_get_thumbnail_size": "A catalog text or image accessor whose name is built in two steps from a class's literal _stem. It is reached whenever a catalog item exists; this host's music catalog is empty and picture bytes are platform-pending, so no test can reach one.",
  "cna_album_copy_thumbnail": "A catalog text or image accessor whose name is built in two steps from a class's literal _stem. It is reached whenever a catalog item exists; this host's music catalog is empty and picture bytes are platform-pending, so no test can reach one.",
  "cna_artist_get_name_size": "A catalog text or image accessor whose name is built in two steps from a class's literal _stem. It is reached whenever a catalog item exists; this host's music catalog is empty and picture bytes are platform-pending, so no test can reach one.",
  "cna_artist_copy_name": "A catalog text or image accessor whose name is built in two steps from a class's literal _stem. It is reached whenever a catalog item exists; this host's music catalog is empty and picture bytes are platform-pending, so no test can reach one.",
  "cna_genre_get_name_size": "A catalog text or image accessor whose name is built in two steps from a class's literal _stem. It is reached whenever a catalog item exists; this host's music catalog is empty and picture bytes are platform-pending, so no test can reach one.",
  "cna_genre_copy_name": "A catalog text or image accessor whose name is built in two steps from a class's literal _stem. It is reached whenever a catalog item exists; this host's music catalog is empty and picture bytes are platform-pending, so no test can reach one.",
  "cna_playlist_get_name_size": "A catalog text or image accessor whose name is built in two steps from a class's literal _stem. It is reached whenever a catalog item exists; this host's music catalog is empty and picture bytes are platform-pending, so no test can reach one.",
  "cna_playlist_copy_name": "A catalog text or image accessor whose name is built in two steps from a class's literal _stem. It is reached whenever a catalog item exists; this host's music catalog is empty and picture bytes are platform-pending, so no test can reach one.",
  "cna_picture_get_image_size": "A catalog text or image accessor whose name is built in two steps from a class's literal _stem. It is reached whenever a catalog item exists; this host's music catalog is empty and picture bytes are platform-pending, so no test can reach one.",
  "cna_picture_copy_image": "A catalog text or image accessor whose name is built in two steps from a class's literal _stem. It is reached whenever a catalog item exists; this host's music catalog is empty and picture bytes are platform-pending, so no test can reach one.",
  "cna_picture_get_thumbnail_size": "A catalog text or image accessor whose name is built in two steps from a class's literal _stem. It is reached whenever a catalog item exists; this host's music catalog is empty and picture bytes are platform-pending, so no test can reach one.",
  "cna_picture_copy_thumbnail": "A catalog text or image accessor whose name is built in two steps from a class's literal _stem. It is reached whenever a catalog item exists; this host's music catalog is empty and picture bytes are platform-pending, so no test can reach one.",
  "cna_get_abi_version": "Called by the loader's own bootstrap through a name held in a variable, before any recording wrapper exists to observe it.",
  "cna_video_player_get_texture": "Superseded in the facade by the frame descriptor that carries the decode generation. Retained for the ownership stress tool, which exercises the bare borrowed-handle route directly."
 }
}
